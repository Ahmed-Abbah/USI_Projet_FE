export enum Fonction {
    MANAGER = 'MANAGER',
    EMPLOYEE = 'EMPLOYEE',
}

export enum Role {
    ADMIN = 'ADMIN',
    USER = 'USER',
}

export enum TypeQuestion {
    MULTIPLE_CHOICE = 'MULTIPLE_CHOICE',
    OPEN_ENDED = 'OPEN_ENDED',
}
